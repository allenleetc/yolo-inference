name: "run-yolo-inference"
type: plugin
version: "0.1.0"
description: "A Python-only plugin to run YOLO inference with a confidence threshold"
fiftyone:
  version: ">=1.2.0"

operators:
  - run-yolo-inference